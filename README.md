install python
python --version
mkdir kirana-app
cd kirana-app
pip install flask flask-cors
create a folder name with Kiranapp and copy the below files from githubsite
app.py
index.html
Using VS Code (recommended)
Open VS Code
Click File → Open Folder
Select kirana-app
run python app.py
open the link in browser http://127.0.0.1:5000
